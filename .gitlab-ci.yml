image: ruby:2.3

dimension_jekyll_theme:
  stage: build
  script:
  - gem install jekyll
  - jekyll build -d public
  - gem build dimension_jekyll_theme.gemspec
  artifacts:
    paths:
    - "public/*"
    - "dimension_jekyll_theme-*.gem"
  only:
  - master